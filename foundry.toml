[profile.default]
src = "src"
out = "out"
libs = ['node_modules', 'lib']
optimizer = true
optimizer_runs = 1_000
# See more config options https://github.com/foundry-rs/foundry/tree/master/config

[rpc_endpoints]
mumbai = "${MUMBAI_RPC_URL}"
bsctestnet = "${BSCTESTNET_RPC_URL}"
fuji = "${FUJI_RPC_URL}"

goerli = "${GOERLI_RPC_URL}"
sepolia = "${SEPOLIA_RPC_URL}"
ethmainnet = "${ETH_RPC_URL}"

[etherscan]
# anvil = {key = "blacksmith", url = "http://localhost:3000/api/verify/" }
goerli = { key = "${ETHERSCAN_API_KEY}" }
# mumbai = { key = "${POLYGONSCAN_API_KEY}", chain = "polygon-mumbai" }
# bsc = { key = "BSCSCAN_API_KEY", url = "https://api-testnet.bscscan.com/" }
